global:
  resolve_timeout: 5m

route:
  group_by: ["alertname", "job"]
  group_wait: 30s
  group_interval: 2m
  repeat_interval: 30m
  receiver: ops-webhook

receivers:
  - name: ops-webhook
    webhook_configs:
      - url: http://host.docker.internal:18080/alerts
        send_resolved: true
